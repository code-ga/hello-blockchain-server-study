name: Build-ts-to-js
on: 
  push:
    paths:
      - "**.ts"
      - "**.json"
      - "**.yml"
    branches:
      - ws
jobs:
  build-ts-to-js:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run : echo "hello word"
    